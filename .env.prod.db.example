# ============================================
# POSTGRESQL DATABASE ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.prod.db and fill in the actual values
# NEVER commit .env.prod.db to version control!
#
# These variables are used by the PostgreSQL Docker container
# They MUST match the database settings in .env.prod

# ============================================
# PostgreSQL Configuration
# ============================================
# Database name - should match DB_NAME in .env.prod
POSTGRES_DB=insurance_broker_prod

# Database user - should match DB_USER in .env.prod
POSTGRES_USER=postgres

# Database password - should match DB_PASSWORD in .env.prod
# Use a strong password with at least 20 characters
# Include uppercase, lowercase, numbers, and special characters
POSTGRES_PASSWORD=your-strong-database-password-here

# ============================================
# IMPORTANT NOTES
# ============================================
# 1. The password here MUST be identical to DB_PASSWORD in .env.prod
# 2. Use a strong, randomly generated password
# 3. Never use default passwords like 'postgres' or 'password'
# 4. Generate a secure password using:
#    python -c 'import secrets; print(secrets.token_urlsafe(32))'
# 5. Keep this file secure and never commit it to version control
