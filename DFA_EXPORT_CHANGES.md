# Изменения в экспорте "Пролонгация" для выделения неактивного ДФА

## Описание изменений

Модифицирован экспорт "Пролонгация" (`PolicyExpirationExporter`) для выделения полисов с неактивным ДФА.

## Внесенные изменения

### 1. Модификация метода `get_row_data()` в `PolicyExpirationExporter`

**Файл:** `apps/reports/exporters.py`

**Изменения:**
- Добавлена проверка статуса ДФА (`policy.dfa_active`)
- В столбец "Примечания" добавляется текст "ДФА закрыт" для полисов с неактивным ДФА
- Сохранена существующая логика для полисов без участия брокера ("без брокера")
- При наличии обеих проблем примечания объединяются через "; " (например: "без брокера; ДФА закрыт")

### 2. Модификация метода `apply_formatting()` в `PolicyExpirationExporter`

**Файл:** `apps/reports/exporters.py`

**Изменения:**
- Добавлена желтая заливка (`FFFF99`) для строк с неактивным ДФА
- Приоритет заливки: желтая (неактивный ДФА) > серая (без участия брокера)
- Если ДФА неактивен, строка выделяется желтым независимо от участия брокера
- Если ДФА активен, но нет участия брокера, строка выделяется серым (как раньше)

## Логика работы

| ДФА активен | Участие брокера | Заливка строки | Примечания |
|-------------|-----------------|----------------|------------|
| ❌ Нет      | ✅ Да          | 🟡 Желтая     | "ДФА закрыт" |
| ❌ Нет      | ❌ Нет         | 🟡 Желтая     | "без брокера; ДФА закрыт" |
| ✅ Да       | ❌ Нет         | ⚪ Серая      | "без брокера" |
| ✅ Да       | ✅ Да          | Без заливки   | (пусто) |

## Тестирование

Проведено тестирование всех четырех сценариев:
- ✅ Корректное формирование примечаний
- ✅ Правильная приоритизация заливки
- ✅ Сохранение существующей функциональности
- ✅ Отсутствие синтаксических ошибок

## Файлы изменены

- `apps/reports/exporters.py` - основные изменения в `PolicyExpirationExporter`

## Обратная совместимость

Все изменения обратно совместимы:
- Существующая логика для полисов без участия брокера сохранена
- Добавлена новая функциональность без нарушения старой
- Не изменены сигнатуры методов или структура данных

## Дата изменений

18 декабря 2025 г.
