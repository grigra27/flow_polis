# Руководство по пост-миграционной проверке

## Обзор

Этот документ содержит полный чеклист проверок, которые необходимо выполнить после миграции Insurance Broker System на новый сервер Timeweb Cloud. Следуйте этим шагам последовательно, чтобы убедиться, что все компоненты системы работают корректно.

**Новая инфраструктура:**
- **Домен**: polis.insflow.ru
- **IP адрес**: 109.68.215.223
- **Хостинг**: Timeweb Cloud

---

## 1. Проверка HTTPS доступа

### 1.1 Базовая проверка доступности

Проверьте, что сайт доступен по HTTPS и возвращает корректный ответ:

```bash
# Проверка HTTP статуса
curl -I https://polis.insflow.ru

# Ожидаемый результат: HTTP/2 200 или 301/302 (редирект на login)
```

### 1.2 Проверка SSL сертификата

Убедитесь, что SSL сертификат валиден и правильно настроен:

```bash
# Проверка сертификата
openssl s_client -connect polis.insflow.ru:443 -servername polis.insflow.ru < /dev/null 2>/dev/null | openssl x509 -noout -dates -subject

# Проверка через curl
curl -vI https://polis.insflow.ru 2>&1 | grep -E "SSL|certificate|subject"
```

**Что проверить:**
- ✅ Сертификат выдан для polis.insflow.ru
- ✅ Сертификат не истек (срок действия > 30 дней)
- ✅ Сертификат выдан Let's Encrypt
- ✅ Нет предупреждений о безопасности в браузере

### 1.3 Проверка HTTPS редиректа

Убедитесь, что HTTP автоматически перенаправляется на HTTPS:

```bash
# Проверка редиректа
curl -I http://polis.insflow.ru

# Ожидаемый результат: HTTP/1.1 301 Moved Permanently
# Location: https://polis.insflow.ru/
```

### 1.4 Проверка заголовков безопасности

Проверьте наличие важных заголовков безопасности:

```bash
# Проверка заголовков
curl -I https://polis.insflow.ru | grep -E "Strict-Transport-Security|X-Frame-Options|X-Content-Type-Options"
```

**Ожидаемые заголовки:**
- `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
- `X-Frame-Options: DENY`
- `X-Content-Type-Options: nosniff`

---

## 2. Тестирование функциональности входа

### 2.1 Проверка страницы входа

1. Откройте браузер и перейдите на https://polis.insflow.ru
2. Убедитесь, что страница входа загружается корректно
3. Проверьте, что все стили (CSS) применены правильно
4. Проверьте, что логотипы и изображения отображаются

**Чеклист:**
- [ ] Страница входа загружается без ошибок
- [ ] CSS стили применены корректно
- [ ] Форма входа отображается правильно
- [ ] Нет ошибок в консоли браузера (F12 → Console)

### 2.2 Вход с существующими учетными данными

Войдите в систему используя учетные данные, которые существовали до миграции:

1. Введите имя пользователя и пароль
2. Нажмите кнопку "Войти"
3. Убедитесь, что вход выполнен успешно
4. Проверьте, что отображается правильное имя пользователя

**Чеклист:**
- [ ] Вход с существующими учетными данными работает
- [ ] После входа отображается главная страница/дашборд
- [ ] Имя пользователя отображается корректно
- [ ] Сессия сохраняется (не требуется повторный вход при обновлении страницы)

### 2.3 Проверка прав доступа

Проверьте, что права доступа пользователей сохранились:

1. Войдите под пользователем с правами администратора
2. Проверьте доступ к административной панели (/admin/)
3. Войдите под обычным пользователем
4. Убедитесь, что ограничения доступа работают

**Чеклист:**
- [ ] Администраторы имеют доступ к /admin/
- [ ] Обычные пользователи не имеют доступа к административным функциям
- [ ] Права доступа соответствуют настройкам до миграции

### 2.4 Проверка выхода из системы

1. Нажмите кнопку "Выйти"
2. Убедитесь, что вы перенаправлены на страницу входа
3. Попробуйте вернуться на защищенную страницу
4. Убедитесь, что требуется повторный вход

---

## 3. Проверка доступности данных полисов и клиентов

### 3.1 Проверка списка полисов

```bash
# Проверка количества полисов в базе данных
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec -T db psql -U postgres -d insurance_broker_prod -c 'SELECT COUNT(*) FROM policies_policy;'"
```

**В веб-интерфейсе:**
1. Перейдите в раздел "Полисы"
2. Убедитесь, что список полисов загружается
3. Проверьте, что количество полисов соответствует ожидаемому
4. Проверьте фильтрацию и поиск

**Чеклист:**
- [ ] Список полисов отображается
- [ ] Количество полисов соответствует данным до миграции
- [ ] Фильтры работают корректно
- [ ] Поиск по полисам работает
- [ ] Пагинация работает (если применимо)

### 3.2 Проверка деталей полиса

1. Откройте детальную страницу любого полиса
2. Проверьте, что все поля отображаются корректно
3. Проверьте, что связанные данные (клиент, страховщик) загружаются
4. Проверьте, что прикрепленные файлы доступны

**Чеклист:**
- [ ] Детали полиса отображаются полностью
- [ ] Все поля содержат корректные данные
- [ ] Связанные объекты (клиенты, страховщики) отображаются
- [ ] Прикрепленные документы доступны для скачивания
- [ ] История изменений сохранена (если применимо)

### 3.3 Проверка списка клиентов

```bash
# Проверка количества клиентов в базе данных
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec -T db psql -U postgres -d insurance_broker_prod -c 'SELECT COUNT(*) FROM clients_client;'"
```

**В веб-интерфейсе:**
1. Перейдите в раздел "Клиенты"
2. Убедитесь, что список клиентов загружается
3. Проверьте корректность отображения данных
4. Проверьте поиск по клиентам

**Чеклист:**
- [ ] Список клиентов отображается
- [ ] Количество клиентов соответствует данным до миграции
- [ ] Контактная информация отображается корректно
- [ ] Поиск по клиентам работает

### 3.4 Проверка создания новых записей

Создайте тестовые записи для проверки функциональности:

1. Создайте тестового клиента
2. Создайте тестовый полис
3. Отредактируйте созданные записи
4. Удалите тестовые записи

**Чеклист:**
- [ ] Создание нового клиента работает
- [ ] Создание нового полиса работает
- [ ] Редактирование записей работает
- [ ] Удаление записей работает
- [ ] Валидация форм работает корректно

### 3.5 Проверка страховщиков

```bash
# Проверка количества страховщиков
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec -T db psql -U postgres -d insurance_broker_prod -c 'SELECT COUNT(*) FROM insurers_insurer;'"
```

**Чеклист:**
- [ ] Список страховщиков отображается
- [ ] Логотипы страховщиков загружаются
- [ ] Данные страховщиков корректны

---

## 4. Проверка работы Celery workers

### 4.1 Проверка статуса Celery контейнеров

```bash
# Проверка запущенных контейнеров
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml ps celery_worker celery_beat"
```

**Ожидаемый результат:**
- Оба контейнера должны быть в состоянии "Up"

### 4.2 Проверка логов Celery worker

```bash
# Просмотр последних логов Celery worker
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs --tail=50 celery_worker"
```

**Что проверить:**
- ✅ Нет ошибок подключения к Redis
- ✅ Worker успешно подключен к брокеру
- ✅ Нет критических ошибок (ERROR, CRITICAL)
- ✅ Задачи обрабатываются (если есть в очереди)

### 4.3 Проверка логов Celery beat

```bash
# Просмотр последних логов Celery beat
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs --tail=50 celery_beat"
```

**Что проверить:**
- ✅ Scheduler запущен успешно
- ✅ Периодические задачи регистрируются
- ✅ Нет ошибок планирования

### 4.4 Проверка подключения к Redis

```bash
# Проверка Redis
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec redis redis-cli ping"

# Ожидаемый результат: PONG
```

### 4.5 Проверка очереди задач

```bash
# Проверка количества задач в очереди
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec redis redis-cli llen celery"
```

**Чеклист:**
- [ ] Celery worker контейнер запущен
- [ ] Celery beat контейнер запущен
- [ ] Redis доступен и отвечает
- [ ] Нет критических ошибок в логах Celery
- [ ] Задачи обрабатываются (если применимо)

### 4.6 Тестирование выполнения задачи (опционально)

Если в системе есть функция, которая запускает фоновую задачу (например, отправка email, генерация отчета), протестируйте её:

1. Запустите действие, которое создает фоновую задачу
2. Проверьте логи Celery worker на наличие задачи
3. Убедитесь, что задача выполнена успешно

---

## 5. Проверка логов на отсутствие критических ошибок

### 5.1 Проверка логов Django приложения

```bash
# Просмотр логов веб-приложения
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs --tail=100 web"

# Поиск ошибок
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs web | grep -E 'ERROR|CRITICAL|Exception'"
```

**Что проверить:**
- ✅ Нет необработанных исключений
- ✅ Нет ошибок подключения к базе данных
- ✅ Нет ошибок 500 Internal Server Error
- ✅ Нет предупреждений о безопасности

### 5.2 Проверка логов базы данных

```bash
# Просмотр логов PostgreSQL
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs --tail=50 db"
```

**Что проверить:**
- ✅ База данных запущена успешно
- ✅ Нет ошибок подключения
- ✅ Нет ошибок репликации или целостности данных

### 5.3 Проверка логов Nginx

```bash
# Просмотр логов Nginx
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs --tail=50 nginx"

# Проверка access логов
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec nginx tail -n 50 /var/log/nginx/access.log"

# Проверка error логов
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec nginx tail -n 50 /var/log/nginx/error.log"
```

**Что проверить:**
- ✅ Нет ошибок конфигурации
- ✅ Запросы обрабатываются корректно (статус 200, 301, 302)
- ✅ Нет массовых ошибок 502 Bad Gateway или 504 Gateway Timeout

### 5.4 Проверка системных логов Django

```bash
# Проверка файлов логов Django (если настроены)
ssh root@109.68.215.223 "cd ~/insurance_broker && tail -n 100 logs/django.log"
ssh root@109.68.215.223 "cd ~/insurance_broker && tail -n 100 logs/security.log"
```

**Чеклист:**
- [ ] Логи веб-приложения не содержат критических ошибок
- [ ] Логи базы данных в норме
- [ ] Логи Nginx не показывают проблем
- [ ] Системные логи Django чистые

---

## 6. Команды для мониторинга здоровья системы

### 6.1 Проверка статуса всех контейнеров

```bash
# Полный статус всех контейнеров
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml ps"

# Проверка здоровья контейнеров
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml ps --format json | jq '.[] | {name: .Name, status: .Status, health: .Health}'"
```

**Ожидаемый результат:**
Все контейнеры должны быть в состоянии "Up" и "healthy" (если настроены health checks).

### 6.2 Проверка использования ресурсов

```bash
# Использование CPU и памяти контейнерами
ssh root@109.68.215.223 "docker stats --no-stream --format 'table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}\t{{.MemPerc}}'"

# Использование дискового пространства
ssh root@109.68.215.223 "df -h"

# Использование дискового пространства Docker
ssh root@109.68.215.223 "docker system df"
```

**Нормальные значения:**
- CPU: < 70% в среднем
- Memory: < 80% использования
- Disk: < 80% использования

### 6.3 Проверка подключений к базе данных

```bash
# Количество активных подключений к PostgreSQL
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec -T db psql -U postgres -d insurance_broker_prod -c 'SELECT count(*) FROM pg_stat_activity;'"
```

# Список активных подключений
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec -T db psql -U postgres -d insurance_broker_prod -c 'SELECT datname, usename, application_name, state FROM pg_stat_activity WHERE datname = '\''insurance_broker_prod'\'';'"
```

### 6.4 Проверка времени отклика

```bash
# Измерение времени отклика главной страницы
curl -o /dev/null -s -w "Time: %{time_total}s\nHTTP Code: %{http_code}\n" https://polis.insflow.ru

# Измерение времени отклика API endpoint (если применимо)
curl -o /dev/null -s -w "Time: %{time_total}s\nHTTP Code: %{http_code}\n" https://polis.insflow.ru/api/health/
```

**Нормальное время отклика:** < 500ms для большинства страниц

### 6.5 Проверка размера базы данных

```bash
# Размер базы данных
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec -T db psql -U postgres -d insurance_broker_prod -c 'SELECT pg_size_pretty(pg_database_size('\''insurance_broker_prod'\''));'"

# Размер таблиц
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec -T db psql -U postgres -d insurance_broker_prod -c 'SELECT schemaname, tablename, pg_size_pretty(pg_total_relation_size(schemaname||'\''.'\'\'||tablename)) AS size FROM pg_tables WHERE schemaname NOT IN ('\''pg_catalog'\'', '\''information_schema'\'') ORDER BY pg_total_relation_size(schemaname||'\''.'\'\'||tablename) DESC LIMIT 10;'"
```

### 6.6 Мониторинг в реальном времени

```bash
# Мониторинг логов в реальном времени
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs -f"
```

# Мониторинг конкретного сервиса
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs -f web"

# Остановить мониторинг: Ctrl+C
```

### 6.7 Скрипт комплексной проверки здоровья

Создайте скрипт для быстрой проверки всех компонентов:

```bash
#!/bin/bash
# health-check.sh

echo "=== Проверка здоровья Insurance Broker System ==="
echo ""

echo "1. Проверка HTTPS доступа..."
curl -s -o /dev/null -w "Status: %{http_code}, Time: %{time_total}s\n" https://polis.insflow.ru

echo ""
echo "2. Проверка контейнеров..."
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml ps"

echo ""
echo "3. Проверка Redis..."
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec redis redis-cli ping"

echo ""
echo "4. Проверка базы данных..."
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec -T db psql -U postgres -c 'SELECT version();'"

echo ""
echo "5. Проверка использования ресурсов..."
ssh root@109.68.215.223 "docker stats --no-stream --format 'table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}'"

echo ""
echo "=== Проверка завершена ==="
```

Использование:

```bash
chmod +x health-check.sh
./health-check.sh
```

---

## 7. Дополнительные проверки

### 7.1 Проверка статических файлов

1. Откройте любую страницу сайта
2. Откройте DevTools (F12) → Network
3. Обновите страницу
4. Проверьте, что все статические файлы загружаются (CSS, JS, изображения)

**Чеклист:**
- [ ] CSS файлы загружаются (статус 200)
- [ ] JavaScript файлы загружаются (статус 200)
- [ ] Изображения загружаются (статус 200)
- [ ] Нет ошибок 404 для статических файлов

### 7.2 Проверка медиа файлов

```bash
# Проверка наличия медиа директории
ssh root@109.68.215.223 "ls -lh ~/insurance_broker/media/"

# Проверка размера медиа файлов
ssh root@109.68.215.223 "du -sh ~/insurance_broker/media/"
```

**В веб-интерфейсе:**
1. Откройте страницу с логотипом страховщика
2. Убедитесь, что изображение отображается
3. Попробуйте загрузить новый файл (если применимо)

### 7.3 Проверка email отправки (если настроено)

Если в системе настроена отправка email:

1. Запустите действие, которое отправляет email (например, сброс пароля)
2. Проверьте логи на наличие ошибок отправки
3. Проверьте получение email

```bash
# Проверка логов email отправки
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs web | grep -i email"
```

### 7.4 Проверка backup системы

```bash
# Проверка наличия backup скриптов
ssh root@109.68.215.223 "ls -lh ~/insurance_broker/scripts/backup-*.sh"

# Тестовый запуск backup (опционально)
ssh root@109.68.215.223 "cd ~/insurance_broker && bash scripts/backup-db.sh"
```

### 7.5 Проверка SSL автообновления

```bash
# Проверка настройки certbot
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs certbot | tail -n 20"
```

# Проверка срока действия сертификата
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml exec certbot certbot certificates"
```

---

## 8. Финальный чеклист

### 8.1 Критические проверки (обязательно)

- [ ] ✅ HTTPS доступ работает без предупреждений
- [ ] ✅ SSL сертификат валиден и не истекает в ближайшие 30 дней
- [ ] ✅ Вход в систему работает с существующими учетными данными
- [ ] ✅ Все контейнеры запущены и в состоянии "Up"
- [ ] ✅ База данных доступна и содержит все данные
- [ ] ✅ Полисы отображаются корректно
- [ ] ✅ Клиенты отображаются корректно
- [ ] ✅ Celery workers работают без ошибок
- [ ] ✅ Логи не содержат критических ошибок
- [ ] ✅ Создание/редактирование записей работает

### 8.2 Важные проверки (рекомендуется)

- [ ] Статические файлы загружаются корректно
- [ ] Медиа файлы доступны
- [ ] Поиск и фильтрация работают
- [ ] Права доступа пользователей корректны
- [ ] Использование ресурсов в норме (CPU < 70%, RAM < 80%)
- [ ] Время отклика приемлемое (< 500ms)
- [ ] Backup система настроена

### 8.3 Опциональные проверки

- [ ] Email отправка работает (если настроено)
- [ ] Отчеты генерируются корректно (если применимо)
- [ ] Интеграции с внешними сервисами работают (если применимо)
- [ ] Мобильная версия отображается корректно

---

## 9. Действия при обнаружении проблем

### Если обнаружены критические ошибки:

1. **Немедленно документируйте проблему**
   - Сделайте скриншоты
   - Сохраните логи
   - Запишите шаги для воспроизведения

2. **Оцените серьезность**
   - Критическая: система недоступна или данные потеряны
   - Высокая: основная функциональность не работает
   - Средняя: некоторые функции не работают
   - Низкая: косметические проблемы

3. **Для критических проблем - рассмотрите откат**
   - См. документ ROLLBACK_PLAN.md
   - Откатите DNS на старый сервер
   - Восстановите старую конфигурацию

4. **Для некритических проблем - исправьте**
   - Проверьте конфигурацию
   - Проверьте логи для диагностики
   - Внесите необходимые изменения
   - Повторите проверки

### Общие решения проблем:

| Проблема | Возможное решение |
|----------|-------------------|
| 502 Bad Gateway | Перезапустите контейнер web: `docker-compose restart web` |
| База данных недоступна | Проверьте credentials в .env файлах, перезапустите db контейнер |
| Celery не работает | Проверьте подключение к Redis, перезапустите celery контейнеры |
| Статические файлы не загружаются | Запустите `collectstatic`: `docker-compose exec web python manage.py collectstatic --noinput` |
| SSL ошибки | Проверьте конфигурацию nginx, перевыпустите сертификат |

---

## 10. Мониторинг после миграции

### Первые 24 часа

Проводите проверки каждые 2-4 часа:

```bash
# Быстрая проверка
ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml ps"
curl -I https://polis.insflow.ru
```

### Первая неделя

Проводите проверки ежедневно:
- Проверяйте логи на ошибки
- Мониторьте использование ресурсов
- Проверяйте feedback от пользователей

### Долгосрочный мониторинг

Настройте автоматический мониторинг:
- Uptime мониторинг (например, UptimeRobot)
- Мониторинг производительности (например, New Relic, Datadog)
- Алерты на критические ошибки
- Регулярные backup'ы базы данных

---

## 11. Контакты и поддержка

### В случае проблем:

1. **Проверьте документацию:**
   - MIGRATION_GUIDE.md
   - SERVER_SETUP.md
   - DNS_SSL_SETUP.md
   - ROLLBACK_PLAN.md

2. **Проверьте логи:**
   ```bash
   ssh root@109.68.215.223 "cd ~/insurance_broker && docker-compose -f docker-compose.prod.yml logs"
   ```

3. **Обратитесь к команде поддержки:**
   - Предоставьте описание проблемы
   - Приложите логи
   - Укажите шаги для воспроизведения

---

## Заключение

После успешного прохождения всех проверок из этого чеклиста, миграция считается завершенной. Система работает на новой инфраструктуре Timeweb Cloud и готова к использованию.

**Следующие шаги:**
1. Мониторьте систему в течение первых 24-48 часов
2. Соберите feedback от пользователей
3. После стабилизации (3-7 дней) можно вывести из эксплуатации старый сервер
4. Обновите документацию с любыми изменениями или найденными решениями

**Поздравляем с успешной миграцией! 🎉**
