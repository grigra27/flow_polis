# 🚀 Быстрый старт развертывания

## Ваша конфигурация

- **Droplet IP:** `64.227.75.233`
- **Домен:** `onbr.site`
- **SSH:** `ssh root@64.227.75.233`

## Развертывание за 3 шага

### 1. Проверьте SSH подключение

```bash
ssh root@64.227.75.233
```

Если подключение работает, выйдите:
```bash
exit
```

### 2. Запустите автоматическое развертывание

```bash
./scripts/initial-deploy.sh
```

IP уже прописан по умолчанию, просто следуйте инструкциям скрипта.

### 3. Проверьте результат

После завершения откройте в браузере:
- https://onbr.site
- https://onbr.site/admin/

---

## Что делает скрипт?

1. ✅ Копирует файлы на Droplet
2. ✅ Создает .env.prod с безопасными паролями
3. ✅ Запускает Docker контейнеры
4. ✅ Получает SSL сертификат от Let's Encrypt
5. ✅ Настраивает HTTPS
6. ✅ Проверяет работоспособность

**Время:** ~15-20 минут

---

## Полезные команды

### Подключение к серверу
```bash
ssh root@64.227.75.233
cd /opt/insurance_broker
```

### Просмотр статуса
```bash
docker compose -f docker-compose.prod.yml ps
```

### Просмотр логов
```bash
docker compose -f docker-compose.prod.yml logs -f
```

### Перезапуск сервиса
```bash
docker compose -f docker-compose.prod.yml restart web
```

---

## Если что-то пошло не так

### Проверьте DNS
```bash
dig onbr.site +short
# Должен вернуть: 64.227.75.233
```

### Проверьте Docker на Droplet
```bash
ssh root@64.227.75.233 "docker --version"
```

### Проверьте firewall
```bash
ssh root@64.227.75.233 "sudo ufw status"
# Должны быть открыты: 22, 80, 443
```

---

## Дополнительная документация

- 📖 [Полное руководство](docs/INITIAL_DEPLOYMENT_GUIDE.md)
- ✅ [Контрольный список](DEPLOYMENT_CHECKLIST.md)
- 📋 [Быстрая справка](DEPLOYMENT_QUICK_REFERENCE.md)
- 📝 [Резюме Task 19](TASK_19_SUMMARY.md)

---

## Конфигурационный файл

Все настройки хранятся в `.env.deployment`:
```bash
DROPLET_IP=64.227.75.233
DROPLET_USER=root
DOMAIN=onbr.site
```

Вы можете изменить эти значения при необходимости.

---

**Готовы начать?** Запустите:
```bash
./scripts/initial-deploy.sh
```
