# Быстрый деплой мониторинга ошибок через Telegram

## 🚀 Для деплоя через GitHub + Docker

### 1. Пуш изменений в репозиторий

```bash
git add .
git commit -m "Add Telegram error monitoring system"
git push origin main
```

### 2. На сервере: пулл и пересборка

```bash
# Подключитесь к серверу
ssh root@your-server-ip

# Перейдите в директорию проекта
cd /root/insurance_broker

# Получите последние изменения
git pull origin main

# Пересоберите контейнер (psutil будет установлен автоматически)
docker-compose -f docker-compose.prod.yml build web

# Перезапустите сервисы
docker-compose -f docker-compose.prod.yml up -d
```

### 3. Настройка и тестирование

```bash
# Быстрая настройка (если Telegram уже настроен для бэкапов)
./scripts/setup-error-monitoring.sh

# Или ручное тестирование
docker-compose -f docker-compose.prod.yml exec web python manage.py test_telegram_errors --test-error
```

## ✅ Что получите

- **Автоматические уведомления** о всех критических ошибках Django
- **Мониторинг системы** - проверка БД, диска, памяти
- **Мониторинг логов** - отслеживание файлов в реальном времени
- **Rate limiting** - защита от спама (макс. 10 уведомлений/час)
- **Единый канал** - все в том же Telegram канале что и бэкапы

## 🔧 Настройки в .env.prod

```bash
# Telegram Error Monitoring (добавьте если нет)
TELEGRAM_ERROR_RATE_LIMIT=10
```

## 📱 Примеры уведомлений

### Критическая ошибка Django:
```
🚨 Critical Error Detected

🕐 Time: 2024-01-15 14:30:25 UTC
📊 Level: ERROR
📁 Module: views
🖥 Server: your-server

❗ Error:
Database connection lost during report generation
```

### Проверка системы:
```
⚠️ System Health Check

📊 Status: WARNING
📈 Metrics:
• Database: healthy
• Disk: warning - 85% used
• Memory: healthy
```

Готово! Теперь все критические ошибки автоматически отправляются в Telegram.
