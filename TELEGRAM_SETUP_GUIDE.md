# Telegram Backup Notifications Setup Guide

Это руководство поможет настроить уведомления о бэкапах через Telegram безопасным способом.

## 🔐 Безопасность

**ВАЖНО:** Токены и ID чатов НЕ хранятся в коде! Они загружаются из переменных окружения или `.env.prod` файла, который не попадает в Git.

## 📋 Предварительные требования

1. Telegram аккаунт
2. Доступ к серверу где развернуто приложение
3. Права на редактирование `.env.prod` файла

## 🤖 Шаг 1: Создание Telegram бота

1. Найдите в Telegram бота `@BotFather`
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Придумайте имя бота (например: "Insurance Backup Bot")
   - Придумайте username (например: "insurance_backup_bot")
4. Сохраните полученный **Bot Token** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## 📱 Шаг 2: Создание канала для уведомлений

1. Создайте приватный канал в Telegram
2. Добавьте созданного бота как администратора канала
3. Отправьте любое сообщение в канал
4. Получите Chat ID:
   - Откройте в браузере: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
   - Замените `<BOT_TOKEN>` на ваш токен
   - Найдите в ответе `"chat":{"id":-1001234567890}` - это ваш Chat ID

## ⚙️ Шаг 3: Настройка на сервере

### Автоматическая настройка (рекомендуется)

```bash
# Подключитесь к серверу
ssh root@your-server-ip

# Перейдите в директорию приложения
cd /root/insurance_broker

# Запустите скрипт настройки
./scripts/setup-telegram.sh
```

Скрипт запросит:
- Bot Token
- Chat ID
- Настройки уведомлений
- Настройки загрузки файлов

### Ручная настройка

Если предпочитаете ручную настройку:

```bash
# Отредактируйте .env.prod
nano .env.prod

# Добавьте или обновите строки:
TELEGRAM_BOT_TOKEN=ваш-токен-бота
TELEGRAM_CHAT_ID=ваш-chat-id
TELEGRAM_ENABLED=true
TELEGRAM_UPLOAD_FILES=true
TELEGRAM_MAX_FILE_SIZE=45
```

## 🧪 Шаг 4: Тестирование

```bash
# Тест подключения к Telegram
./scripts/telegram-notify.sh test

# Тест бэкапа базы данных с уведомлениями
./scripts/backup-db-telegram.sh

# Тест бэкапа медиа файлов с уведомлениями
./scripts/backup-media-telegram.sh
```

## 🔄 Шаг 5: Обновление cron задач

Замените обычные скрипты бэкапов на версии с Telegram:

```bash
# Отредактируйте crontab
crontab -e

# Найдите строки:
# 0 2 * * * cd /root/insurance_broker && /root/insurance_broker/scripts/backup-db.sh >> /root/insurance_broker/logs/backup-db.log 2>&1
# 0 3 * * * cd /root/insurance_broker && /root/insurance_broker/scripts/backup-media.sh >> /root/insurance_broker/logs/backup-media.log 2>&1

# Замените на:
# 0 2 * * * cd /root/insurance_broker && /root/insurance_broker/scripts/backup-db-telegram.sh >> /root/insurance_broker/logs/backup-db.log 2>&1
# 0 3 * * * cd /root/insurance_broker && /root/insurance_broker/scripts/backup-media-telegram.sh >> /root/insurance_broker/logs/backup-media.log 2>&1
```

## 📱 Что вы получите в Telegram

### При запуске бэкапа:
```
🔄 Backup Started

📋 Type: Database Backup
🕐 Time: 2024-01-15 02:00:15 UTC
🖥 Server: your-server-hostname

Starting backup process...
```

### При успешном завершении:
```
✅ Backup Completed Successfully

📋 Type: Database Backup
🕐 Completed: 2024-01-15 02:01:45 UTC
📁 File: db_backup_20240115_020015.sql.gz
📊 Size: 15.2 MB
⏱ Duration: 01:30
🖥 Server: your-server-hostname
```

Плюс файл бэкапа будет загружен в канал.

### При ошибках:
```
❌ Backup Failed

📋 Type: Database Backup
🕐 Time: 2024-01-15 02:00:15 UTC
🖥 Server: your-server-hostname
❗ Error: Database container is not running

Please check the logs for more details.
```

### При очистке старых бэкапов:
```
🧹 Cleanup Completed

📋 Type: Database Backup
🕐 Time: 2024-01-15 04:00:00 UTC
🗑 Deleted: 3 old backup(s)
📅 Retention: 7 days
🖥 Server: your-server-hostname
```

## 🔧 Настройки

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | - |
| `TELEGRAM_CHAT_ID` | ID чата/канала для уведомлений | - |
| `TELEGRAM_ENABLED` | Включить уведомления (true/false) | false |
| `TELEGRAM_UPLOAD_FILES` | Загружать файлы (true/false) | true |
| `TELEGRAM_MAX_FILE_SIZE` | Макс. размер файла в MB | 45 |

### Ограничения Telegram Bot API

- Максимальный размер файла: 50 MB
- Рекомендуемый размер: 45 MB (с запасом)
- Файлы больше лимита будут сжиматься автоматически

## 🚨 Устранение неполадок

### "Telegram connection test failed"
- Проверьте правильность Bot Token
- Убедитесь, что бот добавлен в канал как администратор
- Проверьте правильность Chat ID

### "Failed to send message"
- Проверьте интернет-соединение на сервере
- Убедитесь, что Telegram API доступен
- Проверьте правильность Chat ID

### "File too large for Telegram"
- Увеличьте сжатие бэкапов
- Уменьшите `TELEGRAM_MAX_FILE_SIZE`
- Рассмотрите использование внешнего хранилища для больших файлов

### Бот не отвечает
- Убедитесь, что бот не заблокирован
- Проверьте, что бот имеет права администратора в канале
- Попробуйте отправить боту сообщение напрямую

## 🔒 Безопасность

1. **Никогда не коммитьте токены в Git**
2. **Используйте приватные каналы** для уведомлений
3. **Ограничьте доступ к .env.prod** файлу
4. **Регулярно ротируйте токены** ботов
5. **Мониторьте активность** ботов

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `tail -f logs/backup-*.log`
2. Протестируйте подключение: `./scripts/telegram-notify.sh test`
3. Проверьте переменные окружения: `env | grep TELEGRAM`
4. Пересоздайте бота если необходимо
