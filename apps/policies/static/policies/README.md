# Функция копирования платежей в inline

## Описание

Эта функция добавляет кнопку "📋 Копировать" к каждой строке платежа в inline-таблице на странице редактирования полиса.

## Как использовать

### На странице полиса (рекомендуемый способ)

1. Откройте страницу редактирования полиса в админ-панели:
   ```
   /admin/policies/policy/<id>/change/
   ```

2. Прокрутите вниз до раздела "График платежей"

3. Найдите платеж, который хотите скопировать

4. Нажмите кнопку **"📋 Копировать"** справа от строки платежа

5. Автоматически будет добавлена новая строка с заполненными данными:
   - Порядковый номер года
   - Порядковый номер платежа
   - Дата платежа
   - Сумма платежа
   - Страховая сумма
   - Ставка комиссии
   - Комиссия (руб)
   - Фактическая дата оплаты
   - Дата согласования СК
   - Дополнительная информация

6. Измените нужные поля (например, номер платежа, дату)

7. Нажмите "Сохранить" внизу страницы

### На странице списка платежей (альтернативный способ)

1. Откройте список всех платежей:
   ```
   /admin/policies/paymentschedule/
   ```

2. Выберите платеж(и) галочкой

3. В выпадающем списке действий выберите "Копировать выбранные платежи"

4. Нажмите "Выполнить"

5. Вы будете перенаправлены на форму добавления с предзаполненными данными

6. Измените нужные поля и сохраните

## Технические детали

### Файлы

- **JavaScript**: `apps/policies/static/policies/js/copy_payment_inline.js`
- **CSS**: `apps/policies/static/policies/css/copy_payment_inline.css`
- **Admin**: `apps/policies/admin.py` (класс `PaymentScheduleInline`)

### Как это работает

1. JavaScript добавляет кнопку "Копировать" к каждой строке inline-формы
2. При клике на кнопку:
   - Автоматически нажимается кнопка "Добавить еще один График платежей"
   - Создается новая пустая строка
   - Все поля копируются из исходной строки в новую
   - Страница прокручивается к новой строке
   - Новая строка подсвечивается желтым на 2 секунды

3. Копируются все поля кроме:
   - `id` (автоматически генерируется)
   - `created_at` (устанавливается при сохранении)
   - `updated_at` (устанавливается при сохранении)

### Совместимость

- Django 4.2+
- Работает с Django Admin
- Использует jQuery (встроенный в Django Admin)
- Поддерживает autocomplete поля (Select2)

## Тестирование

Запустите тесты:

```bash
python -m pytest apps/policies/tests/test_inline_copy.py -v
```

Все тесты должны пройти успешно.
