# Настройка Sentry для мониторинга ошибок

## Что такое Sentry?

Sentry - это сервис для мониторинга ошибок в реальном времени. Он автоматически отслеживает все ошибки 500, предоставляет детальную информацию о них и может отправлять уведомления различными способами.

## Быстрая настройка

### 1. Создание аккаунта в Sentry

1. Перейдите на https://sentry.io/
2. Зарегистрируйтесь (бесплатный план включает 5000 ошибок/месяц)
3. Создайте новый проект:
   - Platform: Django
   - Project name: insurance-broker
   - Team: Default

### 2. Получение DSN

После создания проекта вы получите DSN (Data Source Name) - строку вида:
```
https://[key]@[organization].ingest.sentry.io/[project-id]
```

### 3. Настройка в проекте

Добавьте DSN в файл окружения:

**Для production (.env.prod):**
```bash
SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/your-project-id
SENTRY_RELEASE=1.0.0
```

**Для development (.env) - опционально:**
```bash
# SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/your-project-id
# SENTRY_RELEASE=dev
```

### 4. Установка зависимостей

```bash
# Локально
pip install sentry-sdk[django]==1.38.0

# В Docker (уже добавлено в requirements.txt)
docker compose -f docker-compose.prod.yml build web
```

### 5. Перезапуск приложения

```bash
# Development
python manage.py runserver

# Production
docker compose -f docker-compose.prod.yml restart web
```

## Настройка уведомлений

### Telegram уведомления

1. **Создайте Telegram бота:**
   - Напишите @BotFather в Telegram
   - Выполните команду `/newbot`
   - Следуйте инструкциям и получите токен

2. **Получите Chat ID:**
   - Напишите боту любое сообщение
   - Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Найдите `"chat":{"id":123456789}` - это ваш Chat ID

3. **Настройте webhook в Sentry:**
   - Перейдите в Settings → Integrations → Webhooks
   - Добавьте новый webhook:
     ```
     URL: https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage
     Method: POST
     Headers: Content-Type: application/json
     ```
   - Payload Template:
     ```json
     {
       "chat_id": "YOUR_CHAT_ID",
       "text": "🚨 Ошибка в системе полисов!\n\n*Проект:* {{project}}\n*Ошибка:* {{title}}\n*URL:* {{url}}\n*Время:* {{timestamp}}\n\n[Подробнее]({{web_url}})",
       "parse_mode": "Markdown"
     }
     ```

### Slack уведомления

1. **Создайте Slack App:**
   - Перейдите на https://api.slack.com/apps
   - Создайте новое приложение
   - Добавьте Incoming Webhook

2. **В Sentry:**
   - Settings → Integrations → Slack
   - Подключите ваш workspace
   - Выберите канал для уведомлений

### Email уведомления (по умолчанию)

1. **В Sentry:**
   - Settings → Notifications → Email
   - Настройте правила уведомлений:
     - New Issues: Immediately
     - Issue State Changes: Immediately
     - Regression: Immediately

## Настройка правил уведомлений

### Создание умных правил

1. **Перейдите в Settings → Alerts**
2. **Создайте новое правило:**
   - Name: "Critical Errors"
   - Conditions:
     - "An event is seen"
     - "The event's level is equal to error"
   - Actions:
     - "Send a notification via Telegram"
     - "Send a notification via Email"

3. **Создайте правило для частых ошибок:**
   - Name: "Frequent Errors"
   - Conditions:
     - "An event is seen more than 5 times in 1 minute"
   - Actions:
     - "Send a notification via Telegram"

### Фильтрация ошибок

Чтобы не получать спам от незначительных ошибок, настройте фильтры:

1. **В коде (уже настроено):**
   ```python
   # Фильтруем распространенные ошибки
   before_send=lambda event, hint: event if not _should_filter_sentry_event(event, hint) else None
   ```

2. **В Sentry UI:**
   - Settings → Inbound Filters
   - Включите фильтры для:
     - Browser Extensions
     - Legacy Browsers
     - Localhost

## Мониторинг производительности

Sentry также может отслеживать производительность:

```python
# В settings.py уже настроено
traces_sample_rate=0.1  # 10% запросов
```

Это поможет найти медленные запросы и узкие места.

## Полезные функции

### 1. Releases (релизы)

Отслеживайте, в какой версии появились ошибки:

```bash
# При деплое
export SENTRY_RELEASE=$(git rev-parse HEAD)
docker compose -f docker-compose.prod.yml up -d --build
```

### 2. User Context

Sentry автоматически привязывает ошибки к пользователям (если они авторизованы).

### 3. Breadcrumbs

Sentry записывает "хлебные крошки" - последовательность действий перед ошибкой:
- HTTP запросы
- Database queries
- Логи

### 4. Source Maps

Для JavaScript ошибок Sentry может показывать исходный код.

## Проверка работы

### Тестирование интеграции

Добавьте временно в любой view:

```python
def test_sentry(request):
    # Это вызовет ошибку и отправит в Sentry
    division_by_zero = 1 / 0
    return HttpResponse("This won't be reached")
```

### Проверка в Sentry

1. Перейдите в ваш проект в Sentry
2. Должна появиться новая ошибка "ZeroDivisionError"
3. Проверьте, пришло ли уведомление

## Troubleshooting

### Ошибки не попадают в Sentry

1. **Проверьте DSN:**
   ```bash
   echo $SENTRY_DSN
   ```

2. **Проверьте логи:**
   ```bash
   docker compose -f docker-compose.prod.yml logs web | grep -i sentry
   ```

3. **Проверьте сетевое подключение:**
   ```bash
   curl -I https://sentry.io
   ```

### Слишком много уведомлений

1. **Настройте Rate Limiting:**
   - Settings → General → Rate Limiting
   - Установите лимит: 100 events per minute

2. **Используйте Issue Grouping:**
   - Sentry автоматически группирует похожие ошибки
   - Настройте правила группировки в Settings → Processing

### Не приходят уведомления в Telegram

1. **Проверьте токен бота:**
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getMe"
   ```

2. **Проверьте Chat ID:**
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getUpdates"
   ```

3. **Проверьте webhook в Sentry:**
   - Settings → Integrations → Webhooks
   - Посмотрите логи доставки

## Стоимость

- **Free tier:** 5,000 errors/month, 1 user
- **Team plan:** $26/month, 50,000 errors/month, unlimited users
- **Organization:** $80/month, 200,000 errors/month

Для большинства проектов бесплатного плана достаточно.

## Альтернативы

Если Sentry не подходит:

1. **Rollbar** - похожий сервис
2. **Bugsnag** - для мобильных приложений
3. **Собственное решение** - ErrorLog модель в Django (см. основную документацию)

## Заключение

Sentry значительно упрощает отладку production ошибок. Вместо поиска в логах вы получаете:

- Автоматические уведомления о новых ошибках
- Детальную информацию о контексте ошибки
- Группировку похожих проблем
- Статистику по частоте ошибок
- Интеграцию с системами уведомлений

Настройка занимает 30 минут, но экономит часы при отладке проблем в production.
